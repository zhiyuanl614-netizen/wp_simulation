# muni_wdn —— 市政供水管网边界生成器

**角色：上游边界生成器（不与下游耦合求解）。** 用真实基准配水管网 **D-town** 的水力仿真，生成各电厂配水节点的压头轨迹 `H_muni_i(t)`，经"最小供水阈值 28 m"判据得到各电厂供水失效时刻 `t_fault_i`（**多源错峰失压**）。下游冷却水/电力/主动控制模型完全不变，只把失效时刻作为边界参数读入。

## 为什么市政水网只作边界、不耦合进下游

**缓冲位置不对称（关键论证）：** 参照文献气网必须做网络水力仿真，因为缓冲（line pack 管存气）分布在管网内，AET/SAET 由管存气算出。本项目缓冲（可用储水量 ASW）在**电厂内部**（补水箱+集水池），是集总的、不在市政管网里。故市政管网对下游只是一个**压力边界**，不承载下游需要的缓冲物理——用它生成 `H_muni_i(t)` 即充分。

## 为什么用 D-town（而非 C-town）

同一 BWN 系列的 **C-town 导出版 `[JUNCTIONS]` 需水量全为 0、无城市负荷**（校准用拓扑骨架），需人为补背景负荷才能让水箱排空——引入人为成分。**D-town 是 C-town 拓扑的带真实需水量改进版**（2013 BWN 长期改进竞赛）：399 节点中 **348 个有真实需水、5 条日变化需水模式**、7 个分区水箱、单水库总源。城市负荷真实，无需人为补充——这消除了 C-town 版的唯一不真实成分。

## 同源 vs 多源（对齐文献两级案例）

| | 文献 | 本项目 |
|---|---|---|
| 同源 | Fig.5–6：单气源 GS 故障累及多厂 | 单一市政总源/配水点失效累及同源多机（基线，`src/03_proactive_control/` 参数化 `ramp`）|
| 多源错峰 | Fig.7：多端源省级案例，6 机不同 SAET | 市政总源失效经不同 DMA 缓冲，各配水节点错峰失效、各机组不同 SAET（本模块 D-town 真实生成）|

## D-town 基准模型

单水库 R1（市政总源）+ 11 泵 + 7 水箱（T1–T7，分区缓冲）+ 399 节点 + 443 管段 + 5 阀，分层 DMA 结构，含真实城市需水量。三座电厂取水点分处不同 DMA 供区：

| 电厂 | 取水节点 | DMA 供区 | 正常压头 | 故障后失效时刻 |
|---|---|---|---|---|
| bus89 | J411 | 低区(标高 9 m, 弱缓冲) | 65.8 m | +26.0 h |
| bus80 | J371 | 中区(标高 69 m, 中缓冲) | 67.3 m | +28.0 h |
| bus10 | J197 | 高区(标高 42 m, T3 强缓冲) | 73.9 m | +61.0 h |

**故障施加（水源压头下降）：** 市政总源（水库 R1）压头自 t=6 h 起在 3 h 内线性降至近零 → 配水节点压力逐级失效。相比硬切管道更贴合"市政供水压力失效"的物理本意，且保持水力求解良态（D-town 的 11 泵在硬切总源后会进入不稳定工况使 EPANET 求解崩溃）。

## 方法与说明（如实标注）

1. **EPANET 2.2 EPS**：扩展时段准稳态（分钟级），与失压传导尺度（SAET，分钟~小时）一致；水质(水龄)分析关闭，只解水力。
2. **压力驱动需水（PDD）**：压力不足时供水量按物理削减。
3. **真实城市需水量**：D-town 自带 348 节点真实需水 + 5 条日变化模式——图中昼夜起伏即真实日变化需水所致（高区水箱夜间重力供水），无需人为补背景负荷。
4. **失效取首次跌破**：各配水节点随其供区水箱逐级耗尽而先后失效，下游只取首次跌破 28 m 并锁存。

## 运行

```bash
pip install wntr
cd src/00_muni_wdn
python boundary_generator.py     # 仿真 → results/muni_boundary.json（各电厂失效时刻）
python plot_muni.py              # → results/muni_boundary.png（压头轨迹+错峰失效）
python network_outage.py         # 唯一水源停供→全网压力崩溃 → results/network_outage.json
python plot_network_outage.py    # → results/network_outage.png（时间序列+空间快照）
```

## 两类分析

| 模块 | 焦点 | 输出 |
|---|---|---|
| `boundary_generator.py` | **3 个电厂取水点**的错峰失压（多源错峰，供下游 P6 用作边界 t_fault_i）| muni_boundary.{json,png} |
| `network_outage.py` | **唯一水源完全停供**后**全网 399 节点**压力的时空崩溃 | network_outage.{json,png} |

### 唯一水源停供 → 全网压力崩溃（network_outage）

模拟 D-town **唯一水源（水库 R1）完全停止供水**这一确定事件后，**整个管网全部 399 节点压力的时空演化**：失压如何从水源处开始、随各分区水箱逐级放空而在空间上扩散。

- **停供施加（阶跃，自 t=0 起）**：水源 R1 压头自仿真起始时刻即降至近零并保持（唯一水源自始完全停供）。用"压头→0"而非"硬切出水管"：硬切会使 EPANET 稳态解在无水节点算出大量非物理负压（可达 −10⁴ m）；压头→0 + PDD + `minimum_pressure=0` 则物理正确。
- **物理正确性**：自 t=0 停供后压力**并不立即崩溃**——全网 7 个水箱靠储水续供，维持约 12 h（缓冲窗口）；随水箱见底，失压节点占比从初始 ~10% 扩大到 ~62%，**全网半数节点在停供后约 12.5 h 失压**，呈自水源向外的**空间扩散**。
- **可视化**：① 全网压力统计（均值/P10–P90/中位）随时间；② 失压节点占比随时间；③④⑤⑥ 4 个时刻的空间快照（节点坐标着色压力，蓝=健康、红=失压）。

## 数据来源（CC BY-NC 4.0，须署名）

Ostfeld, Avi. *"05 Long Term Improvement" (D-town)* (2016). Battle of the Water Network Models. University of Kentucky Libraries. https://uknowledge.uky.edu/wdst_models/5

数据文件 `data/DTOWN.inp` 按 CC BY-NC 4.0 授权分发，非商业使用，须署名原作者与来源。
